name: Java CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up JDK 17
        with:
          java-version: '17'
      - name: Build with Maven
        run: mvn clean install
      - name: Docker Build & Push
        run: run command